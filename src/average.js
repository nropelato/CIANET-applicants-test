/**
 * This function must return the average value from a sum of two entry numbers
 * To test, just run 'npm run avg' in the console
 * @param a
 * @param b
 * @returns {number}
 */
module.exports = function (a, b) {
    let sum = a + b;
    return sum / 2;
}
